# Demo run record — 2026-09-04 (first autonomous FAIL → PASS)

Nothing below is fabricated. Task rows live in `backend/fixhub.db`.

## Setup
- Model: `z-ai/glm-5.3-free` via `https://api.tokenrouter.com/v1` (bare
  `tokenrouter.com` has no DNS A record — diagnosed via nslookup, base URL fixed).
- Demo repo: `demo/fastapi-jwt`, issue #142 "Expired authentication tokens return HTTP 500".

## What happened (task 17 → 21 in DB)
1. **Reproduce (real Docker):** `FAILED tests/test_expiry.py — assert 500 == 401`.
2. **Investigate:** agent listed files, searched, read `token.py`, `middleware.py`,
   `main.py`, regression test (TOOL events persisted per call).
3. **First fix attempt (task 20):** added `try/except ExpiredSignatureError → 401`
   + `InvalidTokenError → 401` but left a broken line
   (`claims.get("sub") → 500`, a syntax error). Verification suite FAILED —
   the bug was caught by evidence, not by claim. Task → DEBUGGING.
4. **Retry (task 21):** agent removed the broken line, kept the correct handler.
   Verification: `suite PASS, lint PASS` → `READY_FOR_APPROVAL`, `verified: true`.
5. **Independent check:** `demo/fastapi-jwt` `pytest` locally → **1 passed**.

## Bugs found in Fixhub itself during this run (all fixed, all with tests)
- Shell pipes (`| tail`) masked pytest exit codes → no pipes; exit code captured explicitly.
- Assistant `tool_calls` stored in internal shape → provider 400 on next turn →
  provider-native transcript with synthetic ids + `tool_call_id` on tool messages.
- Free model returns `content: null` with answer in `reasoning_content` → fallback.
- Unhandled provider error 500'd the endpoint with task stuck mid-state →
  typed `ProviderError` + retries + FAILED transition (test: `test_orchestrator.py`).
- Agent had no write tools → `edit_file`/`create_file` constrained to workdir
  (tests: `test_edit_tools.py`); read/list hardened against `../` escape.

## Note
After this run the demo repo was reset to the buggy state so the demo is
re-runnable. The agent's fix is preserved in task 21's TOOL events.
